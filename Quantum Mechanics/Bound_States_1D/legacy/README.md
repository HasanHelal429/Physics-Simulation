# Legacy notebooks

The two original 1D finite-difference eigensolver notebooks (double delta
wells, alpha decay). Superseded by `TDSE_Solver/stationary_states.py` (the
dimension-generic grid eigensolver) plus `potentials.double_well` and
`potentials.gamow_barrier`, validated in
`TDSE_Solver/validate_bound_states.py`. Kept for reference.
